import React from 'react'

const Footer = () => {
  return (
    <div className='footer'>
        {/* Unused - will possibly add the pagination to a footer later. */}
    </div>
  )
}

export default Footer